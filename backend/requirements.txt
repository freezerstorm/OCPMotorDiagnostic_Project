# Dépendances Python du backend.
# Installation :  pip install -r requirements.txt
#
# Étape 1 : fastapi, uvicorn, pydantic-settings
# Étape 4 : sqlalchemy, psycopg (PostgreSQL), alembic (migrations)

fastapi>=0.115
uvicorn[standard]>=0.30
pydantic-settings>=2.4

# --- Base de données (Étape 4) ---
SQLAlchemy>=2.0
psycopg[binary]>=3.2     # driver PostgreSQL moderne
alembic>=1.13           # migrations versionnées
